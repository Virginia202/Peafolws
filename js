var anyPrimeNumber
var primeNumbers=[2,3,5,7]
function primeNumber(numbers){
    var index= Math.floor(Math.random()*primeNumbers.length);
    anyPrimeNumber =primeNumbers[index]
}
function identification(userInput){
    primeNumbers(numbers);
}